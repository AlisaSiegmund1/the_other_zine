<% if user_signed_in? %>
  <%= link_to 'edit', edit_issue_path(@issue) %>
  <%= link_to 'delete', issue_path(@issue),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>


<div class="issue-cover">
  <%= cl_image_tag @issue.photo.key, :width=>100, id: "issue-cover-show" %>
</div>

<div class="issue-title">
  <h1>ISSUE #<%= @issue.id %><%=@issue.title %></h1>
</div>

<div class="issue-short-desc">
  <%= @issue.short_description %>
</div>


<div class="new-contribution">
  <%= link_to "new contribution", new_issue_contribution_path(@issue) %>
</div>


<div class="contributions_container">
  <h2>Contributions</h2>

  <% @issue.contribution.contribution_info.each do |info| %>
    <% if info.published == true %>
      <div class="contribution">
        <div class="type-tag">
          <%#= contribution.contribution_type %>
        </div>
        <div class="contribution-content">
          <%# if contribution.contribution_type == 'music' %>
          <a href=<%= contribution.contribution_content %>> <%= contribution.contribution_content %></a>
          <%# else %>
          <%#= contribution.contribution_content %>
          <%# end %>
        </div>
        <div class="contribution-author">
          <%= info.author %>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
