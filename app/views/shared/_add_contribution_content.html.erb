<%= simple_form_for ([@issue, @contribution]) do |f| %>
  <%= f.input :contribution_type, collection: ['listen', 'watch', 'read', 'cooperate', 'view'], label:false%>
  <%= f.input :contribution_content,label:false%>
<% end %>
